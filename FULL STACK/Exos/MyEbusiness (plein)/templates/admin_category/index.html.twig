{% extends 'base.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <h1 class="text-warning mt-3 mb-3 text-center">Categories index</h1>

    <table class="table table-dark table-striped">
        <thead>
            <tr class="text-warning">
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>
                    <button class="btn btn-warning text-warning"><a class="text-decoration-none" <a href="{{ path('app_admin_category_show', {'id': category.id}) }}">show</a></button>
                    <button class="btn btn-warning text-warning"><a class="text-decoration-none" <a href="{{ path('app_admin_category_edit', {'id': category.id}) }}">edit</a></button> 
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<button class="btn btn-warning text-warning"><a class="text-decoration-none" href="{{ path('app_admin_category_new') }}">Create new</a></button>
{% endblock %}
